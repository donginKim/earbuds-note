# 콘텐츠·리스티클 사이트 실험 설계 (T13)

작성 2026-08-09. **탐색적** — 사전등록 실험(9/2 판정)과 분리된 별도 트랙.

## 가설

**H-CONTENT ⚠️**: 콘텐츠·리스티클 사이트를 운영하면 LLM이 이를 인용하고, `utm_source=chatgpt.com` 유입이 발생한다.

## 사전 확률은 낮다 — 시작 전에 명시

기존 실측이 이 가설에 부정적이다.

| 실측 | 결과 |
|---|---|
| 추천·리스티클형이 최저가 쿼리 인라인 | **2/239**, 지배 0 |
| 정보성 질문 검색 발동 | **0/30** — 인용 자리 자체가 없음 |
| "추천으로 신뢰 쌓아 가격 인용" 경로 | 부정됨 |
| 최저가 의도 지배 | 88.9% — 자리가 여기 몰려 있음 |

**그럼에도 검증 가치가 있는 이유**: 기존 데이터는 ChatGPT 무로그인·최저가 의도 중심이고, **리스티클 사이트를 직접 운영해 관측한 적은 없다.** 인용이 아니라 SEO 유입만 나올 가능성이 크고, 그것도 결과다.

## 설계

- 호스팅: `https://donginkim.github.io/earbuds-note` (project site)
- 원본 `donginKim.github.io`는 **건드리지 않는다**
- 발행: GitHub Actions, 매일 07:00 KST. **변경 없으면 완전 no-op**(Last-Modified 보존)
- 콘텐츠: 리스티클 3종(자체 가격 데이터) + 아티클 3종(자체 작성 본문)
- 주제 발굴: 네이버 블로그 검색 API — **제목에서 키워드만** 추출. 본문·요약 미저장

## 결과변수 사다리

```
봇 fetch → 색인 → 후보 진입(출처 패널) → 본문 인용 → utm 유입
```

기존 실험과 동일 구조. 1차 지표는 **색인**이다. 인용을 1차로 잡으면 기저율 0으로 실패한다.

| 지표 | 측정 |
|---|---|
| Z0 색인 | Bing Webmaster · `site:` 조회 |
| Z1 후보 진입 | 주간 프로브에서 출처 패널 등장 |
| Z2 본문 인용 | 인라인 등장 |
| **Z3 utm 유입** | GoatCounter `?src=utm_source` |

## 대조

| 사이트 | 유형 | 호스트 |
|---|---|---|
| `donginkim11st.github.io` | 가격비교형 (기존 개입 실험) | 독립 |
| `donginkim.github.io/earbuds-note` | **콘텐츠·리스티클형** | 원본과 공유 |

**같은 인프라·다른 콘텐츠 유형**이라 유형 간 대조가 된다.

## ⚠️ 한계 (시작 전 기록)

1. **호스트 공유** — 원본 `donginKim.github.io`와 같은 호스트다. 정본 자리를 두고 경쟁할 수 있다. 기존 실험 사이트처럼 독립 호스트가 아니다
2. **SKU 중복** — 기존 실험 사이트와 같은 이어폰 SKU를 다룬다. 서로 경쟁하며 H1~H4를 오염시킬 수 있다. **겹침 여부를 기록하고, 필요 시 SKU 풀을 분리**해야 한다
3. **단일 사이트** — 무작위화 없음. 인과 아님
4. **콘텐츠 품질** — 자동 생성 본문이라 사람이 쓴 글과 다르다. 이것 자체가 교락

## 운영 규칙

- 발행일에 변경이 없으면 **커밋하지 않는다** — 커밋 1건이 전 페이지 Last-Modified를 갱신하는 함정(기존 실험에서 실측)
- 네이버 API 원문 재게시 금지. 키워드만
- 각 문서에 작성일·수집 시각 표기

## 필요한 설정

| 항목 | 위치 |
|---|---|
| 저장소 생성 | `donginKim/earbuds-note`, Pages 소스 지정 |
| `NAVER_CLIENT_ID` / `NAVER_CLIENT_SECRET` | 저장소 Secrets (선택) |
| `INDEXNOW_KEY` | 저장소 Secrets (선택) |
| GoatCounter 코드 | `config.json` |
| `data/sku-data.json` | 기존 실험 사이트에서 복사 |

## 관측 일정

| 시점 | 내용 |
|---|---|
| D+0 | 배포, 기준선 프로브 |
| D+7 | 색인 확인 (Z0) |
| D+30 | 후보 진입 프로브 (Z1) |
| D+60 | 인용·utm (Z2·Z3) |

⚠️ 기존 실험에서 신생 도메인이 서비스 62일 시점에 인용된 사례가 있다. **D+60 이전에 결론 내지 않는다.**
